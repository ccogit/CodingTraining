package org.example;

public enum MelonType {

    TYPE_A, TYPE_B;

//    private static MelonType[] melonTypes = values();

//    public static MelonType getRndMelonType() {
//        return melonTypes[new Random().nextInt(0, values().length)];
//    }
}
